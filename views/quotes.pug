extends layout

block content

  include navbar

  .container.text-sm-center
    h1.display-3 #{quotation}
    .text-sm-right
      h2 - #{source}
    .justify-content-center
      a.btn.btn-outline-light.btn-lg.bg-warning(href="/quotes") New Quote